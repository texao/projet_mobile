package com.example.projet_mobile.candidat;

import android.os.Bundle;
import android.widget.ListView;

import androidx.appcompat.app.AppCompatActivity;

import java.util.ArrayList;

public class CandidaturesEnCoursActivity extends AppCompatActivity {
/*
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.listecandidaturesencours);

        // Récupérer la référence de la ListView
        ListView listeCandidaturesEnCours = findViewById(R.id.listeCandidaturesEnCours);

        // Créer un adaptateur pour afficher les données des candidatures
        CandidaturesAdapter adapter = new CandidaturesAdapter(this, getCandidaturesEnCours());

        // Définir l'adaptateur sur la ListView
        listeCandidaturesEnCours.setAdapter(adapter);
    }

    // Méthode fictive pour obtenir les candidatures en cours
    private List<Candidature> getCandidaturesEnCours() {
        // Implémentez cette méthode pour récupérer les données des candidatures depuis votre source de données
        return new ArrayList<>();
    }


 */
}
